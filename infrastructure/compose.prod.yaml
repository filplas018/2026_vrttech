services:
  web:
    build:
      target: production
    restart: always

  client:
    build:
      target: production
    restart: always
    ports:
      - "80:80"

  caddy:
    volumes:
      - ./Caddyfile.prod:/etc/caddy/Caddyfile:ro

  db:
    restart: always

  redis:
    restart: always
